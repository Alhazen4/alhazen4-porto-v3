services:
  nuxt:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: alhazen4-porto-v3

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: 3000

    restart: unless-stopped